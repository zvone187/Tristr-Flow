{
  "version": 3,
  "task": "Developer ID signing and Apple notarization",
  "status": "release_validation_pending",
  "pass_criteria": [
    "The tagged release job fails closed when Developer ID signing credentials are missing or invalid.",
    "The production app is signed with the current organization's Developer ID Application certificate and hardened runtime.",
    "Apple notarization succeeds and the ticket is stapled before GitHub Release publication.",
    "The published DMG passes codesign, Gatekeeper, and stapler validation after a fresh download.",
    "Local unsigned development packaging remains launchable through the existing ad-hoc fallback."
  ],
  "tests": [
    {
      "id": "E2E-SIGN-01",
      "title": "Release pipeline fails closed without Apple credentials",
      "user_story": "As the product owner, I never publish an unsigned production release by accident.",
      "setup_instrumentation": [
        "Run the release workflow contract tests against the committed GitHub Actions YAML.",
        "Keep the local ad-hoc packaging path separate from the tag-driven production job."
      ],
      "steps": [
        "Confirm the workflow imports a Developer ID Application certificate from encrypted GitHub secrets.",
        "Confirm the workflow reconstructs the App Store Connect API key in the runner's temporary directory.",
        "Confirm electron-builder is invoked with hardened runtime and forceCodeSigning enabled."
      ],
      "expected_results": [
        "The job cannot silently fall back to an unsigned build.",
        "No certificate, password, API key, key ID, issuer ID, or team ID is committed to Git."
      ],
      "proof_to_capture": [
        "Passing release workflow contract test",
        "GitHub Actions log showing signing identity discovery without secret values"
      ],
      "status": "passed",
      "notes": "Workflow contract and entitlements tests pass. The Developer ID signing credential works in a real local production-signing build; all five required Actions secrets are configured."
    },
    {
      "id": "E2E-SIGN-02",
      "title": "Tagged build is signed, notarized, and stapled",
      "user_story": "As a Mac user, I can open Tristr Flow without an unidentified-developer warning.",
      "setup_instrumentation": [
        "Developer ID Application certificate issued to the current Apple organization.",
        "App Store Connect API key with the minimum role required for notarization.",
        "Secrets stored only in the GitHub repository's Actions secrets."
      ],
      "steps": [
        "Push a release tag matching package.json.",
        "Build the arm64 app with hardened runtime and Developer ID signing.",
        "Inspect signed main-app entitlements and verify the Apple Events entitlement and Electron runtime entitlements remain enabled.",
        "Submit the app to Apple's notary service and wait for acceptance.",
        "Staple the ticket, mount the DMG, and run codesign, spctl, and stapler validation on the packaged app."
      ],
      "expected_results": [
        "codesign verification succeeds with the Developer ID chain.",
        "spctl reports the application is accepted.",
        "stapler validates the embedded notarization ticket.",
        "Only verified artifacts are published to the GitHub Release.",
        "Hardened runtime retains the Apple Events capability required by the existing media-control fallback."
      ],
      "proof_to_capture": [
        "Successful GitHub Actions release run",
        "codesign, spctl, and stapler output",
        "Published artifact checksum"
      ],
      "status": "pending",
      "notes": "Initial run failed before publication due to an upstream signing-tool bug. Corrected tool and immutable-tag recovery workflow are under local validation; notarization and production trust gates remain pending."
    },
    {
      "id": "E2E-SIGN-03",
      "title": "Fresh public DMG passes Gatekeeper and launches",
      "user_story": "As a new customer, I can download, install, and start the public Tristr Flow release normally.",
      "setup_instrumentation": [
        "Download the published DMG from the public release URL after the signed release completes.",
        "Preserve normal download quarantine metadata or explicitly assess the mounted app with Gatekeeper."
      ],
      "steps": [
        "Verify the downloaded DMG checksum and disk image integrity.",
        "Mount the DMG and assess the application with codesign, spctl, and stapler.",
        "Launch the app and request the localhost /ping endpoint."
      ],
      "expected_results": [
        "The downloaded artifact matches the published checksum.",
        "Gatekeeper accepts the application without a right-click bypass.",
        "The app launches and returns a healthy ping."
      ],
      "proof_to_capture": [
        "Public download response and checksum",
        "Gatekeeper assessment output",
        "Healthy packaged-app /ping response"
      ],
      "status": "pending",
      "notes": "Awaiting tagged release publication and fresh public DMG verification."
    },
    {
      "id": "E2E-SIGN-04",
      "title": "Local packaging fallback stays usable",
      "user_story": "As a developer, I can still package and run Tristr Flow locally without production credentials.",
      "setup_instrumentation": [
        "Run the ordinary local arm64 packaging command with no CSC or Apple notarization environment variables."
      ],
      "steps": [
        "Build the local app through the existing afterPack hook.",
        "Verify its complete ad-hoc signature.",
        "Launch it and request the localhost /ping endpoint."
      ],
      "expected_results": [
        "The local build remains ad-hoc signed and launchable.",
        "Production signing requirements are confined to the release workflow."
      ],
      "proof_to_capture": [
        "Local electron-builder output",
        "codesign verification",
        "Healthy local /ping response"
      ],
      "status": "partial",
      "notes": "Packaging and signature/integrity checks pass. Fresh launch and ping blocked by an existing listener on port 8757."
    }
  ],
  "validation_notes": [
    "2026-09-16: npm ci completed and enabled Husky; ESLint and all 25 tests pass.",
    "Production dependency audit reports zero vulnerabilities; npm ci reports seven development-dependency findings.",
    "Husky pre-commit entry point passed; changes remain uncommitted pending credentials and full release validation.",
    "Local arm64 DMG/ZIP packaging, plutil validation, deep strict ad-hoc codesign, and DMG integrity checks pass.",
    "A fresh local launch test was not started because port 8757 was already occupied; no existing app was stopped.",
    "Developer ID Application certificate issued after action-time approval; G2 issuer, expiration 2031-09-16. Private key remains in the protected temporary credential directory.",
    "App Store Connect internal-use terms accepted and access approved after action-time approval.",
    "Certificate was saved, matches the prepared private key, and was packaged into an encrypted PKCS12 with the official Apple Developer ID G2 intermediate.",
    "Local Developer ID signed arm64 app build passed after adding the missing G2 intermediate to the certificate export.",
    "Application and native media helper both pass deep strict codesign verification with expected Developer ID authority, hardened runtime, and secure timestamp. Signed main-app Electron and Apple Events entitlements verified.",
    "No commit, tag, push, PR, or new public release has been created.",
    "Final local recheck: npm ci enabled Husky; all 25 tests, ESLint, production dependency audit, and git diff --check pass. Remote main and v0.1.2 tag state remain unchanged.",
    "New API private key found at the user-specified location, validated without displaying contents, and matched to the approved Apple API key ID. A protected credential copy was saved; all five encrypted GitHub Actions secrets are now present.",
    "Release implementation committed as 7c67667 through normal Husky hooks; main fast-forwarded and v0.1.2 pushed atomically. Initial run 35174556374 passed 25 source tests and secret validation but failed before signing/notarization/publication because electron-builder 26.15.3 passes the PKCS12 password instead of the temporary keychain password.",
    "Recovery pins official electron-builder 26.16.1 and adds a manually dispatched workflow that checks out the existing immutable release tag. New contract test failed before implementation. The v0.1.2 tag remains unchanged."
  ]
}
